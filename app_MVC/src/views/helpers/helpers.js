function toUpper(str) {
    return str.toUpperCase();
}

function toLower(str) {
    return str.toLowerCase();
}

module.exports = { toUpper, toLower };

/*create a funcion to convert the string to uppercase
function toUpper(str) {
    str.toString();
    return str.toUpperCase();
}

//create a funcion to convert the string to lowercase
function toLower(str) {
    str.toString();
    return str.toLowerCase();
}

//export
*/

//module.exports = { toUpper, toLower };

// Path: PW/app_MVC/src/views/helpers/helpers.js